<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>{{meta.title}}</title>
  <style>
    @font-face {
      font-family: 'MarathiFont';
      src: url('./fonts/NotoSansDevanagari-Regular.ttf') format('truetype');
    }
    body {
      font-family: 'MarathiFont', sans-serif;
    }
    body {
      font-family: Arial, Helvetica, sans-serif;
      font-size: 11px;
      margin: 30px;
      color: #000;
    }
    h1, h2, h3 {
      text-align: center;
      margin: 4px 0;
    }
    .meta {
      margin: 20px 0;
      font-size: 11px;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 10px;
    }
    th, td {
      border: 1px solid #000;
      padding: 4px 6px;
      text-align: left;
      vertical-align: top;
    }
    th {
      background-color: #e6e6e6;
      font-weight: bold;
      text-align: center;
    }
    .footer-table {
      margin-top: 40px;
    }
    .footer-table td {
      height: 60px;
      text-align: center;
    }
    .note {
      margin-top: 15px;
      font-size: 10px;
      font-style: italic;
      text-align: center;
    }
  </style>
</head>

<body>

  <h1>GUEST MANAGEMENT SYSTEM (GMS)</h1>
  <h2>{{meta.title}}</h2>
  <h3>{{meta.location}}</h3>

  <div class="meta">
    <strong>Period:</strong>
    {{labels.fromDate}}: {{ddmmyyyy meta.fromDate}}
    {{#if meta.toDate}} {{labels.toDate}}: {{ddmmyyyy meta.toDate}}{{/if}}
    &nbsp;&nbsp;&nbsp;
    <strong>Report ID:</strong> {{meta.reportId}}
  </div>


  <table>
    <thead>
      <tr>
        <th>Guest Name & Designation</th>
        <th>Room No.</th>
        <th>Housekeeper</th>
        <th>Butler</th>
        <th>Food / Remarks</th>
        <th>Vehicle No.</th>
        <th>Driver Name</th>
        <th>Pickup - Drop</th>
        <th>Messenger</th>
        <th>Network</th>
        <th>Stay From</th>
        <th>Stay To</th>
        <th>Total Days</th>
        <th>Visit Type</th>
        <th>Exceptions</th>
      </tr>
    </thead>

    <tbody>
    {{#each rows}}
    <tr>
      <td>{{guest_name}} {{designation}}</td>
      <td>{{room_no}}</td>
      <td>{{housekeeper}}</td>
      <td>{{butler}}</td>
      <td>{{food_remarks}}</td>
      <td>{{vehicle_no}}</td>
      <td>{{driver_name}}</td>
      <td>{{pickup_drop}}</td>
      <td>{{messenger}}</td>
      <td>{{wifi_provider}}</td>

      <td>{{ddmmyyyy stay_from}}</td>
      <td>{{ddmmyyyy stay_to}}</td>
      <td>{{total_days}}</td>
      <td>{{visit_purpose}}</td>
      <td>{{exceptions}}</td>
    </tr>
    {{/each}}
    </tbody>

  </table>

  <table class="footer-table">
    <tr>
      <td>Prepared By<br/><br/>(GMS Admin)</td>
      <td>Verified By<br/><br/>(Secretary, Raj Bhawan)</td>
      <td>Approved By<br/><br/>Honourable Governor of Maharashtra</td>
    </tr>
    <tr>
      <td>Signature:</td>
      <td>Signature:</td>
      <td>Signature:</td>
    </tr>
    <tr>
      <td>Date:</td>
      <td>Date:</td>
      <td>Date:</td>
    </tr>
  </table>

  <div class="note">
    This is a system-generated report from the Guest Management System (GMS), Raj Bhawan, Maharashtra.
  </div>

</body>
</html>
