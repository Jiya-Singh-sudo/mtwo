<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>{{meta.title}}</title>
  <style>
    @font-face {
      font-family: 'MarathiFont';
      src: url('./fonts/NotoSansDevanagari-Regular.ttf') format('truetype');
    }
    body {
      font-family: 'MarathiFont', sans-serif;
    }
    body {
      font-family: Arial, Helvetica, sans-serif;
      font-size: 11px;
      margin: 30px;
      color: #000;
    }
    h1, h2, h3 {
      text-align: center;
      margin: 4px 0;
    }
    .meta {
      margin: 20px 0;
      font-size: 11px;
      text-align: center;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 12px;
    }
    th, td {
      border: 1px solid #000;
      padding: 6px;
      text-align: left;
    }
    th {
      background-color: #e6e6e6;
      font-weight: bold;
      text-align: center;
    }
    .footer-table {
      margin-top: 40px;
      width: 100%;
    }
    .footer-table td {
      border: 1px solid #000;
      height: 60px;
      text-align: center;
      vertical-align: middle;
    }
    .note {
      margin-top: 15px;
      font-size: 10px;
      font-style: italic;
      text-align: center;
    }
  </style>
</head>

<body>

  <h1>GUEST MANAGEMENT SYSTEM (GMS)</h1>
  <h2>{{meta.title}}</h2>
  <h3>{{meta.location}}</h3>

  <div class="meta">
    <strong>Period:</strong>
    {{labels.fromDate}}: {{ddmmyyyy meta.fromDate}} – {{labels.toDate}}: {{ddmmyyyy meta.toDate}}
    &nbsp;&nbsp;&nbsp;
    <strong>Report ID:</strong> {{meta.reportId}}
  </div>

  <table>
    <thead>
      <tr>
        <th>Guest Name</th>
        <th>Room Allotted</th>
        <th>Housekeeper Allotted</th>
        <th>Cleaning Type</th>
        <th>Check-In</th>
        <th>Check-Out</th>
        <th>Remarks</th>
      </tr>
    </thead>

    <tbody>
      {{#each rows}}
      <tr>
        <td>{{guest_name}}</td>
        <td>{{room_no}}</td>
        <td>{{housekeeper}}</td>
        <td>{{cleaning_type}}</td>
        <td>{{ddmmyyyy check_in_date}}</td>
        <td>{{ddmmyyyy check_out_date}}</td>
        <td>{{remarks}}</td>
      </tr>
      {{/each}}
    </tbody>
  </table>

  <table class="footer-table">
    <tr>
      <td>Prepared By<br/><br/>(Housekeeping Officer)</td>
      <td>Verified By<br/><br/>(Secretary, Raj Bhawan)</td>
      <td>Approved By<br/><br/>Honourable Governor of Maharashtra</td>
    </tr>
    <tr>
      <td>Signature:</td>
      <td>Signature:</td>
      <td>Signature:</td>
    </tr>
    <tr>
      <td>Date:</td>
      <td>Date:</td>
      <td>Date:</td>
    </tr>
  </table>

  <div class="note">
    This is a system-generated report from the Guest Management System (GMS), Raj Bhawan, Maharashtra.
  </div>

</body>
</html>
