<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>{{meta.title}}</title>

  <style>
    body {
      font-family: Arial, Helvetica, sans-serif;
      font-size: 11px;
      margin: 30px;
      color: #000;
    }
    h1, h2, h3 {
      text-align: center;
      margin: 4px 0;
    }
    .meta {
      margin: 20px 0;
      font-size: 11px;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 10px;
    }
    th, td {
      border: 1px solid #000;
      padding: 4px 6px;
      vertical-align: top;
    }
    th {
      background-color: #e6e6e6;
      text-align: center;
      font-weight: bold;
    }
    .footer-table {
      margin-top: 40px;
    }
    .footer-table td {
      height: 60px;
      text-align: center;
    }
    .note {
      margin-top: 15px;
      font-size: 10px;
      font-style: italic;
      text-align: center;
    }
  </style>
</head>

<body>

  <h1>GUEST MANAGEMENT SYSTEM (GMS)</h1>
  <h2>{{meta.title}}</h2>
  <h3>{{meta.location}}</h3>

  <div class="meta">
    <strong>Period:</strong>
    {{ddmmyyyy meta.fromDate}}
    {{#if meta.toDate}} – {{ddmmyyyy meta.toDate}}{{/if}}
    &nbsp;&nbsp;&nbsp;
    <strong>Report ID:</strong> {{meta.reportId}}
  </div>

  <table>
    <thead>
      <tr>
        <th>Trip Date</th>
        <th>Guest Name</th>
        <th>Driver Name</th>
        <th>Vehicle No</th>
        <th>Pickup Location</th>
        <th>Drop Location</th>
        <th>Start Time</th>
        <th>End Time</th>
        <th>Trip Status</th>
        <th>Remarks</th>
      </tr>
    </thead>

    <tbody>
    {{#each rows}}
      <tr>
        <td>{{ddmmyyyy trip_date}}</td>
        <td>{{guest_name}}</td>
        <td>{{driver_name}}</td>
        <td>{{vehicle_no}}</td>
        <td>{{pickup_location}}</td>
        <td>{{drop_location}}</td>
        <td>{{start_time}}</td>
        <td>{{end_time}}</td>
        <td>{{trip_status}}</td>
        <td>{{remarks}}</td>
      </tr>
    {{/each}}
    </tbody>
  </table>

  <table class="footer-table">
    <tr>
      <td>
        Prepared By<br/><br/>
        (GMS Admin)
      </td>
      <td>
        Verified By<br/><br/>
        (Secretary, Raj Bhawan)
      </td>
      <td>
        Approved By<br/><br/>
        Honourable Governor of Maharashtra
      </td>
    </tr>
    <tr>
      <td>Signature:</td>
      <td>Signature:</td>
      <td>Signature:</td>
    </tr>
    <tr>
      <td>Date:</td>
      <td>Date:</td>
      <td>Date:</td>
    </tr>
  </table>

  <div class="note">
    This is a system-generated report from the Guest Management System (GMS), Raj Bhawan, Maharashtra.
  </div>

</body>
</html>
